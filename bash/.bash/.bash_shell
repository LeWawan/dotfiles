# History control
shopt -s histappend
HISTCONTROL=ignoreboth
HISTSIZE=32768
HISTFILESIZE="${HISTSIZE}"

# Autocompletion
source "${HOME}/.bash/completions/completions.bash"

# Ensure command hashing is off for mise
set +h
